'use strict'

const express = require('express')
const router = express().router()

//admin routes
router.post('/admin', require())

//user routes
router.post('/user', require())

module.express = router